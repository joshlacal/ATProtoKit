        public enum Error: String, Swift.Error, CustomStringConvertible { 
            {%- for error in errors %}
                case {{ error.name | lowerCamelCase_with_underscore }} = "{{error.name}}.{{ error.description }}"
            {%- endfor %}
            public var description: String {
                return self.rawValue
            }
        }
